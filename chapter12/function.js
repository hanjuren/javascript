// var f = function add (x, y) {
//   return x + y;
// };

// var result = f(1, 2);

// console.log(result);

console.log(add(1, 3));
// console.log(sub(2, 1));

function add (x, y) {
  return x + y;
}

const sub = (x, y) => {
  return x + y;
}
console.log(sub(2, 1));
